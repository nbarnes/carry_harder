<%= form_for(@chassi) do |f| %>
  <% if @chassi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chassi.errors.count, "error") %> prohibited this chassi from being saved:</h2>

      <ul>
      <% @chassi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
