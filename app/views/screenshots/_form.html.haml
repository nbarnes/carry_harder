= form_for @screenshot, :html => {:multipart => true} do |f|
  - if @screenshot.errors.any?
    #error_explanation
      %h2
        #{pluralize(@screenshot.errors.count, "error")} prohibited this screenshot from being saved:

      %ul
        - @screenshot.errors.full_messages.each do |message|
          %li
            = message

  = f.file_field :image

  .actions
    = f.submit
